{
  "name": "newyorkhut",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "audit": "wrangler deploy --config wrangler.jsonc --dry-run",
    "check": "wrangler deploy --config wrangler.jsonc --dry-run",
    "build": "wrangler deploy --config wrangler.jsonc --dry-run",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "dev": "wrangler dev --config wrangler.jsonc",
    "audit:live": "node scripts/audit-live-sitemap.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "wrangler": "^4.0.0"
  }
}
