{
  "name": "newyorkhut",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "check": "wrangler deploy --dry-run",
    "deploy": "npm run check && wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
